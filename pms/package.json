{
  "name": "pharmacy-management-system",
  "version": "1.0.0",
  "description": "Advanced AI-Powered Pharmacy Management System",
  "scripts": {
    "backend": "cd backend && npm run start:dev",
    "frontend": "cd frontend && npm run dev",
    "mongodb": "cd mongodb-server && npm run start",
    "docker": "docker-compose up -d",
    "prisma": "cd backend && npx prisma studio",
    "seed": "cd backend && npx prisma db seed"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}